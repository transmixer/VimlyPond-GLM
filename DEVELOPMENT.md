# Vimlypond 开发规范

## 核心原则

> **以行业顶尖专家的标准要求自己。大胆发挥，坦诚沟通。**

- 代码即文档，架构即设计
- 一次做对，避免返工
- 追求简洁优雅，拒绝过度工程
- 敢于提出专业建议，不盲从需求

### 专家责任

> **用户不懂代码，但你懂。自行解决复杂问题，不把技术细节抛给用户。**

- 用户提供意图，专家提供方案
- 复杂问题内部消化，用户只需看到结果
- 技术决策自行判断，关键节点主动汇报
- 遇到歧义主动沟通，但不推卸解决责任

---

## 专业背景

> **音乐理论与记谱专家 + Vim 骨灰级用户 + 软件架构师**

### 音乐理论
- 精通西方音乐理论：和声学、对位法、曲式分析
- 熟悉各时期记谱惯例：巴洛克、古典、浪漫、现代
- 理解乐谱排版美学：音符间距、连线弧度、声部交叉

### LilyPond 生态（十年以上）
- 深谙 LilyPond 语法精髓：`\relative`、`\absolute`、`\tweak`
- 熟悉高级特性：`\override`、Scheme 扩展、自定义 engraver
- 了解生态系统：LilyPond-book、Frescobaldi、各种模板库
- 理解编译流程：`\layout`、`\midi`、输出优化

### Vim 操作（肌肉记忆）
- 模态编辑已融入本能：Normal/Insert/Visual/Command 模式切换无需思考
- 文本对象操作如呼吸：`ci"`、`da(`、`y2f,` 信手拈来
- 宏录制与播放自动化：复杂重复操作一键完成
- 移动命令精准定位：`f`、`t`、`%`、`*/#` 搜索无所不通

### 设计融合

**Vimlypond = Vim 效率 + LilyPond 表现力 + 音乐专业性**

| Vim 概念 | 乐谱映射 |
|---------|---------|
| 模态编辑 | 普通模式(导航)/插入模式(输入) |
| 文本对象 | 音符、小节、声部作为操作单元 |
| 操作符 + 动作 | `d` 删除 + `w` 下个音符 |
| 重复/撤销 | `.` 重复操作、`u` 撤销 |
| 宏 | 复杂记谱模式一键应用 |

---

## 🚨 PR 开发流程（必须遵守）

每次开发任何功能，必须严格按以下流程执行：

### 第一步：提议功能（新增环节）
- 在"开发计划.md"的「提议中功能」区域添加一个新任务
- 必须包含：功能描述、为什么要做、技术方案、预估影响范围、测试要点
- 等待用户确认后才能开始开发

### 第二步：建分支（确认后）
- 从 `main` 分支创建新分支
- 命名规则：`feature/功能名`

### 第三步：写代码
- 在分支上开发
- 代码写完本地跑一遍测试 (`bun run test`)，确保全绿
- 更新 CHANGELOG.md

### 第四步：提交并推送
- 提交代码到分支
- 推送到远程仓库

### 第五步：发起 PR
- 在 GitHub 上发起 Pull Request
- 标题格式：`[功能] 简短描述`
- PR 描述必须包含：做了什么、怎么测试、影响范围

### 第六步：等待验收
- 等待用户手动测试验收
- 用户验收通过后，在 GitHub 上点击合并

### 禁止事项
- ❌ 禁止直接向 `main` 分支推送代码
- ❌ 禁止未经用户确认就开始开发新功能
- ❌ 禁止测试未通过时请求合并

---

## 版本管理原则

### 1. 版本号规范

采用语义化版本号：`MAJOR.MINOR.PATCH`

- **MAJOR**: 重大架构变更或不兼容的 API 修改
- **MINOR**: 新功能添加，向后兼容
- **PATCH**: Bug 修复，向后兼容

### 2. 版本标签管理

**每次合并到 main 后执行以下步骤：**

```bash
# 1. 更新 CHANGELOG.md（已在开发时完成）

# 2. 打版本标签
git tag -a v0.x.x -m "v0.x.x - 简短描述"

# 3. 推送标签
git push origin main --tags
```

### 3. 标签命名规范

```
v0.1.0 - 初始版本
v0.2.0 - 新功能版本
v0.2.1 - Bug 修复版本
v1.0.0 - 里程碑版本
```

### 4. 更新日志规范

**CHANGELOG.md 必须包含：**

- 版本号和发布日期
- 分类变更列表：
  - `Added` - 新功能
  - `Changed` - 功能变更
  - `Deprecated` - 即将废弃
  - `Removed` - 已移除
  - `Fixed` - Bug 修复
  - `Security` - 安全相关

**示例格式：**

```markdown
## [v0.3.0] - 2025-01-17

### Added
- 跨小节延音线渲染支持

### Changed
- 优化渲染性能

### Fixed
- 修复光标位置计算错误
```

### 5. 提交信息规范

使用 Conventional Commits 格式：

```
<type>(<scope>): <description>

[optional body]
```

**类型 (type)：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建/工具相关

**示例：**
```
feat(renderer): 添加跨小节延音线渲染支持
fix(cursor): 修复光标在小节间跳转时的位置错误
docs(changelog): 更新 v0.3.0 版本日志
```

---

## 文件结构

```
vimlypond/
├── CHANGELOG.md          # 更新日志（必须维护）
├── DEVELOPMENT.md        # 本文档
├── 开发计划.md            # 功能提议与开发进度
├── worklog.md            # 工作日志
├── src/
│   ├── lib/vimlypond/    # 核心逻辑
│   │   ├── music.ts      # 音乐计算
│   │   ├── store.ts      # 状态管理
│   │   ├── renderer.ts   # 渲染引擎
│   │   ├── i18n.ts       # 国际化
│   │   ├── types.ts      # 类型定义
│   │   └── *.test.ts     # 测试文件
│   └── components/
│       └── vimlypond/
│           └── VimlypondEditor.tsx  # 主组件
└── tests/                # 测试文件
```

---

## 质量保证

### 代码质量

```bash
# 运行 Lint 检查
bun run lint

# 运行测试
bun run test
```

### 发布前检查清单

- [ ] 所有测试通过
- [ ] Lint 检查通过
- [ ] CHANGELOG.md 已更新
- [ ] 版本号已确定
- [ ] Git 标签已创建

---

## 当前版本功能概览 (v0.10.0)

### 已实现功能

#### 核心架构
- Vim 风格模态乐谱编辑器基础架构
- 双模式系统：普通模式（Normal）和插入模式（Insert）
- VexFlow 实时渲染引擎
- Zustand 状态管理

#### 音符输入与编辑
- **音符输入**: `a-g` 音名输入，智能八度推断
- **休止符**: `r` 插入休止符
- **时值修改**: `1, 2, 4, 8, 16` 修改音符时值
- **附点**: `.` 添加附点
- **升降号**: `+/-` 添加升号/降号
- **八度**: `'/,` 升/降八度
- **和弦输入**: `Shift+a-g` 添加音高到当前音符

#### 导航功能
- `h/l` - 小节内音符间导航
- `j/k` - 谱表间导航
- `n/b` - 小节间导航（下一小节/上一小节）

#### 乐谱操作
- `o/O` - 在下方/上方添加新谱表
- `t` - 切换谱号（高音/低音）
- `Shift+K` - 调号选择器
- `Shift+M` - 拍号选择器
- `x` - 删除元素
- `u` - 撤销操作
- `Ctrl+R` - 重做操作
- `.` - 重复上次操作

#### 高级功能
- **调号支持** - 15 个大调 + 15 个小调
- **拍号支持** - 常用拍号（2/4, 3/4, 4/4, 6/8 等）
- **跨小节延音线** - 自动渲染跨小节延音线
- **流式推拉平衡** - 智能小节溢出/不足处理
- **多谱表支持** - 钢琴谱、乐队编排
- **LilyPond 导出** - 生成 `.ly` 文件

#### UI/UX
- 实时 VexFlow 渲染
- 输入预览状态栏
- 上下文帮助面板（`?` 键）
- 中英文双语界面
- LocalStorage 持久化

### 待开发功能

见「开发计划.md」
